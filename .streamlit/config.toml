[theme]
primaryColor = "#6366f1"
font = "sans serif"

[server]
headless = true
port = 8502

[browser]
gatherUsageStats = false

[client]
# Disable Streamlit's auto-generated sidebar navigation from the pages/ directory.
# BaseTruth uses its own custom sidebar navigation built with st.button().
hideSidebarNav = true
