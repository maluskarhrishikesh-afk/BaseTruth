# BaseTruth — pinned runtime requirements
# Used by the Docker build for reproducible installs.
# Generated from: pip freeze (venv, 2026-03-22)
# To regenerate: pip freeze | grep -Ei "<package list>" > requirements.txt

# ── PDF extraction ──────────────────────────────────────────────────────────
PyMuPDF==1.27.2.2
pypdf==6.9.1
pypdfium2==5.6.0
pdfminer.six==20251230
pdfplumber==0.11.9
pikepdf==10.5.1

# ── OCR pipeline ─────────────────────────────────────────────────────────────
pytesseract==0.3.13
pdf2image==1.17.0
ocrmypdf==17.4.0
img2pdf==0.6.3
pi-heif==1.3.0

# PaddleOCR — superior accuracy for ID cards (PAN, Aadhaar, passports).
# Requires separate PaddlePaddle install; see https://www.paddlepaddle.org.cn/install
# pip install paddlepaddle paddleocr
# paddlepaddle>=2.6.0         # uncomment if using CPU paddle
# paddleocr>=2.9.1            # uncomment after paddlepaddle is installed

# ── Image analysis ────────────────────────────────────────────────────────────
pillow==12.1.1
opencv-contrib-python==4.13.0.92
ImageHash==4.3.2
PyWavelets==1.9.0
insightface>=0.7.3         # optional: Python ≤ 3.12 only; MediaPipe used as fallback
onnxruntime>=1.17.1        # required by insightface when available
mediapipe>=0.10.0          # face mesh fallback for Python 3.13+

# ── EXIF / metadata ───────────────────────────────────────────────────────────
ExifRead==3.5.1
PyExifTool==0.5.6

# ── Cryptography / signatures ─────────────────────────────────────────────────
cryptography==46.0.5
cffi==2.0.0
pycparser==3.0

# ── Statistics and ML ─────────────────────────────────────────────────────────
numpy==2.4.3
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3
threadpoolctl==3.6.0
faiss-cpu==1.13.2

# VLM / Gemma fallback for OCR (local model or Gemini API).
# One of the following is required to enable the VLM OCR fallback:
#   Option A — Local Gemma model:
#     pip install transformers torch accelerate
#     Set GEMMA_MODEL_PATH env var to the model directory.
#   Option B — Gemini API (cloud, no GPU needed):
#     pip install google-generativeai
#     Set GEMINI_API_KEY env var.
# google-generativeai>=0.8.0  # uncomment for Gemini API option

# ── API ───────────────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=12.0,<14
python-multipart>=0.0.9
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
typing-inspection==0.4.2

# WebRTC — lower-latency video streaming for Video KYC.
# Optional: WebSocket fallback is still available if aiortc is not installed.
# pip install aiortc
# aiortc>=1.9.0                # uncomment to enable WebRTC support

# ── Database ─────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# ── Object storage (MinIO / S3-compatible) ────────────────────────────────────
boto3>=1.37.0

# ── UI (Streamlit dashboard) ─────────────────────────────────────────────────
streamlit>=1.42.0
pandas>=2.2.0

# ── Output / formatting ───────────────────────────────────────────────────────
rich==14.3.3
markdown-it-py==4.0.0
mdurl==0.1.2
fpdf2==2.8.7
fonttools==4.62.1

# ── Shared deps ───────────────────────────────────────────────────────────────
lxml==6.0.2
Deprecated==1.3.1
deprecation==2.1.0
wrapt==2.1.2
defusedxml==0.7.1
uharfbuzz==0.53.3
