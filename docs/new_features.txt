
Clean up ->

1) From the left panel remove "Layered Analysis" option and also remove any code related to it. We
are no longer going to use this screen.

Remove these also from the left panel as its of no use to the end customer -> 🟢 PostgreSQL connected

-------------------------------------------------------------------------------------------------

2) Can you move the entire left panel to the top so that all options are visible without 
scrolling down. There is lot of space wasted above BaseTruth logo.

-------------------------------------------------------------------------------------------------

3) We are no longer using these tables in the database ->
Layered Analysis Entries
Cases 
Case Notes

Remove them from the system and also any code refrencing them as its no loger required.
Remove it from "Database Viewer" screen also.
Remove these tables from the documentation also.

-------------------------------------------------------------------------------------------------

4) In the databe for all tables we should capture 
created_on in this format -> Day/Month/Year
created_at in this format -> Hours:Minutes:Seconds

Basically any date and time should be captured in this format only so it becomes easy to work with it.

-------------------------------------------------------------------------------------------------

5) Now I want to test the application with  "GITHUB_TOKEN": "<stored in artifacts/config/settings.json — gitignored>",
so can you make that setting active. Will switch to gemma4 later.

Also tell me how to change the settings so that I will be able to do it myself.

