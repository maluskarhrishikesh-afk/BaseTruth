
Now we have captured lot of data in document_extractions table. 

On Document Intelligence screen we should now have a option to generate final report for every entity

When we click on this generate final report button, we should do the following ->
Compare names on Aadhar, PAN, payslips, marksheets, degrees etc. They all should be same per entity.

Address if present on any other document should match with the one given on Aadhar.

PAN number is present on any other document should match with the PAN number on the PAN Card.

Aadhar number if present on any other document should match witht he Aadhar number on the Aadhar card.

Overall salary mentioned on salary-slips should match with the amount mentioned on offer letter or increment letter.

Forensic details of all documents should be considered.

And a final report should be created. This should again go cases section where it will undergo 
2 levels of approval similar to scans screen.

We will need to create a table for cases functionality which should be similar to scans functionality.

Eventually this final report should be available for download on the Reports screen. Other documents 
should also be available for download in zipped format. final report should be independant, it should
not be included in the zipped file.

Apart from this do you feel I have missed anything that should be included to make this the best
document intelligence product ever.

New table that we create should also be visible on Database Viewer screen.