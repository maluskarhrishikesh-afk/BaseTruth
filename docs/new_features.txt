
Database Viewer ->

Can you remove these legacy columns ->

approved	VARCHAR(10)	Legacy derived field: approved / rejected / NULL
approved_by	VARCHAR(255)	Legacy single-level reviewer name
approved_at	TIMESTAMPTZ	Legacy single-level approval timestamp
approval_comment	TEXT	Legacy single-level reviewer comment


Nothing got saved in scans table.

-------------------------------------------------------------------------------------------------------

Also in "Database Viewer" Danger zone can you provid options to RESET individual tables also?

-------------------------------------------------------------------------------------------------------

After 2nd level approval, nothing got displayed in "Document Intelligence".
The same data displayed in Scans hsould be displayed in Document Intelligence

-------------------------------------------------------------------------------------------------------

In "Bulk Scan" screen, whenever we upload any document first gemma4 should classify the document.
If it is an image file then it should call this code -> image_forensics_detect.py
If it is an pdf file having an image like marksheet, degree etc then also it should call this code -> image_forensics_detect.py

If it is a pdf having structured data like payslips, offer letter etc then it should call forensic code
for pdf. For this just keep a place holder for now we will come back to this.

-------------------------------------------------------------------------------------------------------

Rename records.py to document_intelligence.py
Rename records to document_intelligence everywhere.

-------------------------------------------------------------------------------------------------------

Can you keep all the documentation updated so that we dont loose track of things.

Also run the code from your end to make sure everything is working properly.