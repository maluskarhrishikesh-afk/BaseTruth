I got similar data for pdfs, can you check if its proper or not?

Yes — I checked your PDF forensic training dataset, and it has good feature ideas, but there are several major issues that should be corrected before using it for XGBoost or any ML model.

Overall Verdict
Current quality: 5.5/10

Good direction, but there is label leakage, duplicated filenames, constant useless columns, and unrealistic labeling patterns.

Good Things in Your PDF Dataset ✅
You included strong PDF forensic signals:

incremental updates

EOF marker count

metadata anomalies

hidden text spans

white text spans

JavaScript presence

embedded files

signature gaps

rendered ELA / noise ratios

object count

entropy

These are exactly the kinds of signals used in PDF fraud detection.

Major Problems Found 🚨
1. Same filename exists with opposite labels (Very Dangerous)
I found:

33 filenames appearing as both:

label = 0

label = 1

Why this breaks training:
Model may memorize filename instead of fraud signals.

Fix:
Never train using filename

Use:

X = df.drop(columns=["filename", "label"])
Or rename versions clearly:

invoice_original.pdf

invoice_tampered.pdf

2. heuristic_score likely causing leakage
Correlation with label:

0.53
That’s high.

If heuristic_score is your own rule-engine output, then model just learns your old rules.

Fix:
Decide:

Option A:
Use ML to replace heuristics:

➡️ remove heuristic_score

Option B:
Use ensemble:

➡️ keep score, but know model partly copies rules.

3. Constant Columns (Useless)
These columns never change:

non_embedded_fonts

tiny_size_spans

embedded_files_count

has_xfa_form

obj_stm_count

Fix:
Remove them or populate realistic values.

4. Labels look suspiciously harsh
Examples in your sample:

Rows marked tampered / likely tampered while label = 0

That means either:

labels wrong

heuristic verdict wrong

many false positives

Example:
heuristic_verdict = TAMPERED, but label = 0

This can confuse model.

Fix:
Need clean ground truth labels.

5. Very Small Dataset
Only:

66 rows
That is far too small.

Recommended:
Minimum: 500 rows

Better: 3000+

Best: 10000+

Strong Feature Engineering Advice
Binary Features Good for PDFs
Keep:

has_open_action

javascript_count > 0

hidden spans > 0

white text > 0

signature gaps > 0

Normalize Ratios
Good:

render_ela_suspicious_block_ratio

render_noise_hotspot_ratio

Keep them.

What to Remove for Training
drop_cols = [
 "filename",
 "heuristic_verdict"
]
Maybe also remove:

"heuristic_score"
if you want pure ML.

Best Realistic Fraud Cases for PDFs
Need these in training:

Genuine but suspicious:
scanned PDF

digitally signed file

OCR converted doc

compressed bank statement

print-to-PDF invoice

Fraud but subtle:
edited amount only

changed date

changed payee name

hidden white text

overlay object

removed signature layer

My Honest Verdict
Structure = Good
Data realism = Medium
Ready for Production = No
If You Train XGBoost Right Now
Likely result:

Training accuracy high

Real-world performance poor

Because of duplicate filenames + small data + leakage.

Recommended Next Move
I can generate a Production-Grade PDF Dataset (10,000 rows) with:

✅ real PDF fraud patterns
✅ hidden text attacks
✅ signature fraud
✅ metadata fraud
✅ OCR scan genuine files
✅ balanced labels
✅ hard subtle fraud cases
✅ XGBoost ready

Just say:

Generate PDF Ultimate Dataset


Generate PDF Ultimate Dataset

Your PDF Ultimate Dataset is ready.

Downloads
📄 10,000-row PDF forensic dataset: pdf_ultimate_dataset_10000_rows.csv

📓 XGBoost + SHAP notebook: pdf_xgboost_shap_notebook.ipynb

What’s Included
Dataset Quality
10,000 rows

Balanced labels (5,000 genuine / 5,000 tampered)

Realistic correlated PDF fraud signals

Hard subtle fraud cases (hard_subtle_case=1)

Scanned PDFs, signed PDFs, OCR-converted files

No filename leakage (pdf_id only)

Features Included
incremental_updates

eof_marker_count

metadata_anomaly_score

hidden_text_spans

white_text_spans

javascript_count

embedded_files_count

signature_gap_score

render_ela_suspicious_block_ratio

render_noise_hotspot_ratio

object_count

stream_entropy

xref_mismatch_score

font_switch_score

ocr_text_layer_gap

is_scanned_pdf

has_signature

hard_subtle_case

label

Fraud Types Simulated
Genuine
Native exported PDFs

Scanned PDFs

OCR-generated PDFs

Digitally signed documents

Large complex PDFs

Tampered
Edited amount/date/payee

Hidden white-text injection

Metadata manipulation

XREF/object rewrite fraud

Signature replacement

Overlay tampering

Subtle expert fraud

Recommended Training Code
X = df.drop(columns=["pdf_id", "label"])
y = df["label"]
Best Real-World Evaluation
Check subtle fraud only:

df[df["hard_subtle_case"] == 1]
That tells you if model catches professional fraud.

If you'd like, I can generate the Final Enterprise Version:
🔥 Multi-format Fraud Engine Dataset
Combines:

Images

PDFs

Scans

IDs

Payslips

Invoices

Bank statements

Signatures

With:

✅ 100,000 rows
✅ One universal fraud model
✅ Explainable AI dashboard
✅ Confidence score
✅ FastAPI deployment
✅ Human review routing

Just say Generate Enterprise Fraud Engine 



